/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true,
  },
  output: "export", // If you need to keep this for static exports
};

export default nextConfig;
